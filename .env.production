// .env.production
// Must be the literal "production" — vue-cli only applies production webpack
// mode (minification, hashed filenames, no devtool) when NODE_ENV === "production".
// The previous "prod" value made webpack run in development mode: the bundle was
// shipped UNMINIFIED and full of host environment variables.
NODE_ENV=production